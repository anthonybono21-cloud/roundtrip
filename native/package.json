{
  "name": "roundtrip-native",
  "version": "1.0.0",
  "private": true,
  "description": "Native shells for Roundtrip: iPhone, iPad, Mac and Android.",
  "scripts": {
    "sync-web": "node ./sync-web.mjs",
    "sync": "npm run sync-web && cap sync",
    "android:apk": "npm run sync && cd android && ./gradlew assembleDebug",
    "ios:open": "npm run sync && cap open ios"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0",
    "@capacitor/ios": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
